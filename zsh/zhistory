rm -rf .Zsh-test 
ll. | grep "^l" | awk '{print $1,$9,$10,$11}' | column -t
la | grep '^-'
la | grep '^-' | column -t
la | grep '^-' | awk '{print $1}'
la | grep '^-' | awk '{print $1,$9}'
la | grep '^-' | awk '{print $1,$9,$10,$11}'
la | grep '^-' | awk '{print $1,$9,$10,$11}' | column -t
lf | sort -bd
lf | sort -b
lf | sort -d
man sort
YT
l.
dfc
ls -lh -d .* | grep "^l" | awk '{print $1,$9,$10,$11}' | column -t
cat .zshrc | sed -e '/^#/{n,d}' | wc -l
cat .zshrc | wc -l
vim .zshrc0
eV
tree -L 1
cd tmux
vim conf
vim inx
rm -rf suckless 
la
mkf rtorrentrc
ls .vim
ls .vim/
ls .zsh/
ls .zsh
ls .zs*
git clone git://github.com/robbyrussell/oh-my-zsh.git ~/.oh-my-zsh
cd .oh-my-zsh
lll
vim git-prompt.plugin.zsh
cp g* ~/.vim/plugin/git-prompt/
cp .zshrc .zshrc.orig
rm -rf .zshrc
cp ~/.oh-my-zsh/templates/zshrc.zsh-template ~/.zshrc
cd .vim
cd plugin
mv git-prompt ~/.zsh/
cd .zsh
vim ../vcs_prompt.sh
mv ../vcs_prompt.sh ./
cd .config/etc/dotfiles/zsh
mkdir plugins
cd plugins
mkdir git-prompt
cp ~/.zsh/git-prompt/gitstatus.py ./
cp ~/.zsh/git-prompt/vcs_prompt.sh ./vcs_prompt
cd ~/.zsh
cd git-prompt
vim gitstatus.py
cd .config/etc/dotfiles/zsh/plugins/git-prompt
mkf shot
vim shot
_prompt -l
tree -L 2
tree -L 2 | less
ld zsh/ 
vim .gitignore
cd plugins/git-prompt
cd ....
cd ../..
cd..
cd dotfiles
vim ~/.config/etc/dotfiles/zsh/plugins/git-prompt/vcs_prompt
ld ./zsh/
lf zsh
column -t awk'{print $1,$9}' grep "^d" ll
column -t awk '{print $1,$9}' grep "^d" ll
column -t <(awk '{print $1,$9}' <(grep "^d" <(ll)))
column -t <(awk '{print $1,$9}' <(grep "^d" <(ll zsh)))
ld zsh/
ld zsh
ld 
ld tmp
ls -d .* ~/
ls ~/ -d .*
man ls
ld. .zsh
ls .*
ls -d .*
ld. gitstuff/clone/st
rm -rf .zshrc.orig
vim .zsh-update
cf
Xr
bash .scripts/pipes.sh
c
../st
../dwm
../i3
vim LICENSE
mkf vcs_name
chmod 700 vcs_name
git --help
mux a m
tmux attach -t Main
echo $(git_super_status)
[[ $(git_super_status) == "" ]] && echo 1
[[ $(git_super_status) == "" ]] && echo 1 || echo 2
[[ $(git_super_status) == "" ]] && echo 1 || git rev-parse --show-toplevel
[[ $(git_super_status) == "" ]] && echo "" || git rev-parse --show-toplevel
[[ $(git_super_status) == "" ]] || git rev-parse --show-toplevel
[[ $(git_super_status) == "" ]] || basename `git rev-parse --show-toplevel`
cd gitstuff
cd clone
cd st
echo $__GIT_PROMPT_DIR
cd .scripts
./vcs_name
vim vcs_name
bash ~/.scripts/vcs_name
cd bin
cd etc
tree
man tree
tree -C --dirsfirst
cd ..
cd home
vim Xresources
cd ../zsh/plugins/git-prompt
vim vcs_prompt
vim zshrc
sudo apt-get install rtorrent
updatedb
sudo updatedb
locate rtorrent
ls /usr/share/doc/rtorrent
ncdu
ncdu ..
sudo apt-get install cmatrix
cmatrix
cd zsh
basename `git rev-parse --show-toplevel`
git rev-parse --show-toplevel
cd ../home
cd ../mutt
mkf procmailrc
mkf muttrc
mkf offlineimaprc
lf.
ld ..
git status
git status | awk '/fatal: Not a git/'
git status | awk '/fatal\: Not a git/'
cd gitstuff/clone/st
git status | awk '/Not a git/'
[[ $(git status | awk '/Not a git/') == "" ]] && echo 1 || echo 2
xrdb -merge ~/.Xresources
cd gitstuff/clone/dwm
ll.
vim BUGS
vim config.h
sudo make clean install
git apply ./statuscolors.diff
git -3 apply ./statuscolors.diff
git apply -3 ./statuscolors.diff
cd .config/etc/dotfiles
lf
cd
sudo apt-get install herbstluftwm
cd vim
cd colors
...
cd mpd
cd mpd ; ld
mkf mpd.conf
cd ncmpcpp ; ld
..
exit
ld.
eZ
Z
dot
vim link-teh-dots
q
lsblk
ls
ld
li
C
li.
vim .xinitrc
ll
systemctl list-units
systemctl disable lightdm.service
sudo systemctl disable lightdm.service
sudo systemctl enable lightdm.service
Q
